Еще почему-то в реальном майне они невидимые, когда надуваются (или мб я что-то не дотестил?).
  - Попробовал на майне со сборкой, с которой играли, когда случился баг - не повторилось. Мб шейдеры?

Я пока что костыльно уменьшаю спаун водных и адских/эндовых желеблобов,
прописав шансы в SpawnerType'ах и дополнительные условия в checkJellyBlobSpawnRules.
Однако, мне стоило бы переработать мой CustomNaturalSpawner так, чтобы шансы спауна
прописывались не у SpawnerType'ов, а чтобы можно было указывать шансы спауна прямо
в json-файлах (думаю, как свойство AddCustomSpawnsBiomeModifier можно сделать).

Желеблобы спаунятся все с одинаковым углом, но, судя по всему, если перезайти,
то они оказываются уже под случайным углом. Скорее всего, дело в том, что угол,
под которым они спаунятся, просто не передается на клиент сразу же. Скорее всего,
он передается только когда моб поворачивается (чего не происходит).

Добавить поддержку модов (через loot modifiers)
А что, если коробка будет генерироваться не за время, а за экспу?
Например, нажимаешь shift+ПКМ по генератору, он отнимает 30 уровней (если в руке нет предмета!)
и дает рандомную коробку (или в зависимости от числа уровней шансы менять)

Пофиксить то, что между несколькими игроками, смотрящими в интерфейс пазла, обновляется только
mainInventory (потому что он в единственном экземпляре, передается через констркутор Container'а,
а остальные инвентари у каждого контейнера свои создаются). Можно попробовать переместить
все инвентари в TileEntity и передавать контейнеру сам TileEntity.

Еще был баг, когда я вышел из интерфейса головоломки, у Наташи сбросился инвентарь головоломки
в изначальное состояние, хотя это должно было произойти только если все игроки закрыли инвентарь.

Сделать обычные ящики более разнообразными

Можно добавить ящики, в которых будет пачка разных предметов из одной определенной категории
Добавить ящики с только плохим/только хорошим?

Проклятье с отложенным эффектом
Проклятье из пачки отрицательных эффектов
Наложение чар на имеющиеся у игрока предметы (позитивные, негативные)
Можно придумать новые зачары (в противовес существующим, к примеру Прочность -> Хрупкость)
Благословение (много чего можно придумать попробовать)
Чума - отравление на всех поблизости



/execute at @e[type=q_craft:stone_crab] run tp @p ~ ~ ~
/kill @e[type=q_craft:stone_crab]



https://github.com/skylinerw/guides/tree/master/java



ЧТОБЫ СДЕЛАТЬ JAR, НУЖНО:
1. Прописать в консоли gradlew build (.\gradlew build)
2. Готовый .jar файл лежит в build/libs